<%- include('../partials/header') %>

<div class="page-split">

  <!-- Sale Form -->
  <div class="card">
    <div class="card-header">
      <span class="card-title">Record Sale</span>
    </div>
    <div class="card-body">
      <form method="POST" action="/sales" id="sales-form">
        <div class="form-group">
          <label class="form-label">Customer</label>
          <select name="BakeryID" class="form-control" required>
            <option value="">— Select Customer —</option>
            <% bakeries.forEach(b => { %>
              <option value="<%= b.BakeryID %>"><%= b.BakeryName %></option>
            <% }) %>
          </select>
        </div>
        <div class="form-group">
          <label class="form-label">Product</label>
          <select name="ProductID" id="ProductID" class="form-control">
            <option value="">— Select Product —</option>
            <% products.forEach(p => { %>
              <option value="<%= p.ProductID %>">
                <%= p.ProductName %> (<%= p.Unit %>) — Stock: <%= p.Stock %>
              </option>
            <% }) %>
          </select>
          <div class="form-hint" style="font-size:0.82rem;color:var(--text-muted);margin-top:6px">
            Leave product blank to record a payment-only entry.
          </div>
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px">
          <div class="form-group">
            <label class="form-label">Quantity</label>
            <input type="number" id="Quantity" name="Quantity" class="form-control"
              step="0.01" min="0.01" placeholder="0.00">
          </div>
          <div class="form-group">
            <label class="form-label">Unit Price (Rs)</label>
            <input type="number" id="UnitPrice" name="UnitPrice" class="form-control"
              step="0.01" min="0" placeholder="0.00">
          </div>
        </div>
        <div class="form-group">
          <label class="form-label">Total Amount</label>
          <div id="calc-total" style="font-family:'Playfair Display',serif;font-size:1.4rem;
            color:var(--primary);font-weight:700;padding:4px 0">Rs 0.00</div>
        </div>
        <div class="form-group">
          <label class="form-label">Amount Paid (Rs)</label>
          <input type="number" name="PaidAmount" id="PaidAmount" class="form-control"
            step="0.01" min="0" placeholder="0.00">
        </div>
        <div class="form-group">
          <label class="form-label">Sale Date</label>
          <input type="date" name="SaleDate" class="form-control"
            value="<%= new Date().toISOString().split('T')[0] %>" required>
        </div>
        <div class="form-group">
          <label class="form-label">Notes (Optional)</label>
          <input type="text" name="Notes" class="form-control" placeholder="Any remarks...">
        </div>
        <button type="submit" class="btn btn-primary">✓ Record Sale</button>
      </form>
    </div>
  </div>

  <!-- Sales History -->
  <div class="card">
    <div class="card-header">
      <span class="card-title">Sales History</span>
      <span style="font-size:0.82rem;color:var(--text-muted)"><%= sales.length %> entries</span>
    </div>
    <!-- Filter bar -->
<!-- Filter bar -->
<div style="padding:12px 16px;background:var(--surface2);border-bottom:1px solid var(--border)">
  <form method="GET" action="/sales" style="display:flex;gap:10px;flex-wrap:wrap;align-items:flex-end">
    <div>
      <label class="form-label" style="margin-bottom:3px">From</label>
      <input type="date" name="from" class="form-control" style="width:145px" value="<%= filters.from||'' %>">
    </div>
    <div>
      <label class="form-label" style="margin-bottom:3px">To</label>
      <input type="date" name="to" class="form-control" style="width:145px" value="<%= filters.to||'' %>">
    </div>
    <div>
      <label class="form-label" style="margin-bottom:3px">Bakery</label>
      <select name="bakery" class="form-control" style="width:150px">
        <option value="">All</option>
        <% bakeries.forEach(s => { %>
          <option value="<%= s.BakeryID %>" <%= filters.bakery==s.BakeryID?'selected':'' %>>
            <%= s.BakeryName %>
          </option>
        <% }) %>
      </select>
    </div>
    <div>
      <label class="form-label" style="margin-bottom:3px">Product</label>
      <select name="product" class="form-control" style="width:150px">
        <option value="">All</option>
        <% products.forEach(p => { %>
          <option value="<%= p.ProductID %>" <%= filters.product==p.ProductID?'selected':'' %>>
            <%= p.ProductName %>
          </option>
        <% }) %>
      </select>
    </div>
    <div style="display:flex;gap:8px;align-items:flex-end">
      <button type="submit" class="btn btn-primary" style="width:auto;padding:9px 16px">Filter</button>
      <a href="/sales" class="btn" style="background:var(--surface);border:1.5px solid var(--border);padding:9px 14px">Clear</a>
    </div>
  </form>
</div>

    <div class="table-wrap">
      <% if (sales.length === 0) { %>
        <div class="empty-state"><div class="icon">💰</div><p>No sales recorded yet.</p></div>
      <% } else { %>
      <table>
        <thead>
          <tr><th>Date</th><th>Bakery</th><th>Product</th><th>Qty</th><th>Price</th><th>Total</th><th>Paid</th><th>Due</th></tr>
        </thead>
        <tbody>
          <% sales.forEach(s => { %>
          <tr>
            <td class="muted"><%= new Date(s.SaleDate).toLocaleDateString('en-PK') %></td>
            <td style="font-weight:500"><%= s.BakeryName %></td>
            <td><%= s.ProductName %></td>
            <td><%= s.Quantity %> <%= s.Unit %></td>
            <td class="muted">Rs <%= Number(s.UnitPrice).toLocaleString('en-PK') %></td>
            <td style="font-weight:600;color:var(--success)">
              Rs <%= Number(s.TotalAmount).toLocaleString('en-PK') %>
            </td>
            <td class="muted">Rs <%= Number(s.PaidAmount || 0).toLocaleString('en-PK') %></td>
            <td style="font-weight:600;color:var(--warning)">Rs <%= Number(s.BalanceAmount || 0).toLocaleString('en-PK') %></td>
          </tr>
          <% }) %>
        </tbody>
        <tfoot>
  <tr style="background:var(--surface2)">
    <td colspan="5" style="padding:11px 14px;font-weight:600;text-align:right">Total (<%= sales.length %> records):</td>
    <td style="padding:11px 14px;font-weight:700;color:var(--primary)">
      Rs <%= sales.reduce((s,p)=>s+Number(p.TotalAmount),0).toLocaleString('en-PK') %>
    </td>
    <td style="padding:11px 14px;font-weight:700;color:var(--primary)">
      Rs <%= sales.reduce((s,p)=>s+Number(p.PaidAmount||0),0).toLocaleString('en-PK') %>
    </td>
    <td style="padding:11px 14px;font-weight:700;color:var(--warning)">
      Rs <%= sales.reduce((s,p)=>s+Number(p.BalanceAmount||0),0).toLocaleString('en-PK') %>
    </td>
  </tr>
</tfoot>
      </table>
      <% } %>
    </div>
  </div>

</div>

<%- include('../partials/footer') %>
