<%- include('partials/header') %>
<div class="card" style="max-width:500px;margin:60px auto;text-align:center">
  <div class="card-body">
    <div style="font-size:3rem;margin-bottom:16px">⚠️</div>
    <div style="font-family:'Playfair Display',serif;font-size:1.4rem;color:var(--primary);margin-bottom:8px">
      <%= title %>
    </div>
    <p style="color:var(--text-muted);margin-bottom:20px"><%= message %></p>
    <a href="/" class="btn btn-primary" style="display:inline-flex;width:auto">← Back to Dashboard</a>
  </div>
</div>
<%- include('partials/footer') %>
